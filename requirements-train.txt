# Teacher fine-tuning and student distillation.
#
#   python -m venv .venv-train && . .venv-train/bin/activate
#   pip install -r requirements-train.txt
#
# Keep this separate from requirements-compress.txt: that half needs TensorFlow, which does
# not install cleanly alongside this one.

numpy
pyyaml
h5py            # the dual-readout inputs are HDF5

torch

# The foundation-model backbone.
#
# Pinned, because src/ft_finetune.py depends on two things that are not part of a stable
# public API: the TimesFM_2p5_200M_torch loader, and revin / update_running_stats from
# timesfm.torch.util, which it calls directly to reproduce the preprocessing the forecast
# path applies internally. A release that moves either will break the run, or change the
# preprocessing without failing.
timesfm==2.0.2

# The checkpoint itself is pinned, in the `checkpoint` key of each config:
#   google/timesfm-2.5-200m-pytorch
#
# Loading that checkpoint through `transformers` instead of the `timesfm` package
# random-initializes every weight -- from_pretrained reports the transformer layers and both
# projections as missing -- so the run trains from scratch while appearing to fine-tune.
